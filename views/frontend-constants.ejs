<script>


    const socketProtocol = (window.location.protocol === 'https:' ? 'wss:' : 'ws:');
    const WEBSOCKET_SERVER_ADDR = socketProtocol + '//' + window.location.host;
    const TURN_SERVER_ADDR = window.location.protocol + '//' + window.location.host;

    let webRtcConfiguration = {
        iceServers: [
            /*{
            "urls": `turn:${TURN_SERVER_ADDR}`,
            username: 'client-123',
            credentials: 'pass-pass-word'
        }, */
            {
            "urls": [
                "stun:stun.l.google.com:19302",
                "stun:stun2.l.google.com:19302",
            ],
            username: 'client-123',
            credentials: 'pass-pass-word'
        }
        ]};

        let DEBUG = false;
        <% if (debugMode){ %>
            DEBUG = true;
        <% } %>


<!--    <% if (process.env.NODE_ENV === 'development'){ %>-->
        // const WEBSOCKET_SERVER_ADDR = "ws://localhost:8080";
<!--    <% }else{ %>-->


<!--    <% } %>-->
</script>